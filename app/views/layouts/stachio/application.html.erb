<!DOCTYPE html>
<html>
<head>
  <title>Stachio</title>
  <%= csrf_meta_tags %>
</head>
<body>

  <% if flash.any? %>
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, :class => "flash #{key}") %>
    <% end %>
  <% end %>

  <%= yield %>

</body>
</html>
